import { ArticleQuantityChange } from './article-quantity-change';

describe('ArticleQuantityChange', () => {
  it('should create an instance', () => {
    expect(new ArticleQuantityChange()).toBeTruthy();
  });
});
